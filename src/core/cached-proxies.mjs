import dbCache from "./defines.mjs"; // Use the correct path to your file
import sql from "sqlite3";

const { _ProxyObj } = dbCache;

const GetProxyList = async () => {};

const CacheProxy = async (proxyObj) => {
   proxyObj.name;
};
